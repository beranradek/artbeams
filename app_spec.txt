New features to implement

# Configure kotlin linter in application build

So we can check lint errors after each autocoding session,
or before releases and also fix some automatically.

# Arrow position in testimonial carousel

In testimonial carousel, user images were moved from bottom to top, but the arrow of main bubble remained at bottom, please move it to top as well, so that the bubble arrow points to user image.

See PRPs/improvements_2026_01_11/testimonial_carousel.png
